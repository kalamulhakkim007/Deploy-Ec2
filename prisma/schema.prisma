datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

// Add this model
model User {
  id    Int    @id @default(autoincrement())
  name  String
  email String @unique
  age   Int
}
